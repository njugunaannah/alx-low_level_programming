#include "main.h"

/**
 * main - Entry point
 * description: a programme prints alphabets in lowercase
 * Return: Always 0 (success)
 */

int main(void)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
		putchar('\n');
		return (0);
}
